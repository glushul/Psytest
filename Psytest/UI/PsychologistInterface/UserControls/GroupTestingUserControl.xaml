<UserControl x:Class="Psytest.UI.UserControls.GroupTestingUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Psytest.UI.UserControls"
             mc:Ignorable="d" d:DesignHeight="150" d:DesignWidth="1000" Margin="3, 3, 6, 3" FontFamily="Verdana"
             Width="1000">
    
    <Border BorderThickness="1">
        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="45"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" x:Name="TestingTextBlock" FontSize="20" TextWrapping="Wrap"
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="1" HorizontalAlignment="Center"
                   x:Name="StudentCountTextBlock" FontSize="16" TextAlignment="Center"/>
            <Button x:Name="ButtonGetResults" Grid.Row="3" Content="Сохранить" Click="ButtonPrintResults_Click"/>
        </Grid>
    </Border>
</UserControl>
