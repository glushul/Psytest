<UserControl x:Class="Psytest.UI.UserControls.TestingUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Psytest.UI.UserControls"
             mc:Ignorable="d" Margin="10"
             d:DesignHeight="100" d:DesignWidth="1270">

    <Border Width = "1270" BorderThickness = "1">
        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="TextBlockNumerator" Grid.Column="0" FontSize="16" VerticalAlignment="Top" HorizontalAlignment="Center"/>
            <StackPanel x:Name="MainStackPanel" Grid.Column="1">
                <TextBlock Text="{Binding Description}" FontSize="20" TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
